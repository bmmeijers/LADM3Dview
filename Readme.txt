1. Restore the Database
Ensure you have PostgreSQL installed and restore the database as needed. Configure the database connection information in the `server.js` 

2.Install Project Dependencies
Run the following command : 
npm i

3. Build the Project
Run the following command to build the project, which will generate the dist folder
npm run build

4.Start the Project
Run the following command to start the server: 
node server.js